	# toDo:
	> локальный тест пакета на бд postgres с Delve
		+ уст-ть Delve в Ubuntu
		+ деплой postgreSQL в ubuntu - https://habr.com/ru/articles/578744/
		- дебаг post05.go:
            + сделать из него запускаемый файл: package main, func main
            + перенести туда postGo.go
            + post05/GetSchema.go - протестить подключение к БД
				* в Ubuntu. сохранять состояние виртуалки
            - запуск post95/post05.go как исполняемого файла - проверка работы пакета
				+ сделать правки, чтобы пакет стал исполняемым
				+ отладка
					+ ошибка - отсутстие записей в таблицах. см вывод сообщений  в post05.go -> ListUsers()
						+ добавлена обработка отсутствия записей
					+ вывод список записей:
						* баг по таблице users: если одна запись есть ,то не добавляется. также ошибки
							+ в таблицу userdata добавить запись руками
							+ дебаг
						+ из таблицы userdata
					+ глобальный флаг наличия записей, чтобы не проверять при каждом запросе
					+ добавить глобальную переменную
					+ переделать getRowsCount() в isEmptyDb() bool - проверка на пустоту.
						+ проверка users
						+ проверка userdata
					+ первой запустить её в main
					+ везде заменить вызов isEmptyDb обращением к флагу, либо удалить проверку
					+ удалить из ListUsers
						+ проверить вывод
					+ добавление пользователя
						+ проверка существования записи с этим username перед всеми операциями. если есть, то возможно только изменение и удаление
					+ изменение userdata пользователя
					+ удаление пользователя
				+ причесать имена переменных
				+ запушить то что есть
				- сделать пакетом
					- скомпилировать локально
				- тест работы как с пакетом
				
	- реализовать
		- ввод параметров подключения к бд в команде

# notes
* сейчас нужно каждый раз удалять контейнер, запускать, создавать таблицы. если запускать существующий контейнер, то ошибка:
================
пример команды для работы с БД: 
go run GetSchema.go localhost 5432 postgres mysecretpassword postgres
go run post05.go localhost 5432 postgres mysecretpassword postgres
================
- подключить docker volume - хранение данных после остановки контейнера
	* подключал, но не проверял: каталог хранения volume: /home/dev/docker/volumes/post05_postgres , имя volume: post05_postgres